{
  "_comment": [
    "The Stagenet 'Ledger RC3 compatible' stack. These move TOGETHER — bumping one",
    "alone is unsupported. Source: Midnight Q2 2026 Beta Delivery Document.",
    "",
    "Do NOT copy versions out of the midnight-context snapshot: that snapshot tracks",
    "the PREVIEW line (node 1.0.0, indexer 4.3.2, ledger 8.x) and is a reference for",
    "API shape only. Stagenet is a separate, newer line."
  ],

  "target": "stagenet",

  "stack": {
    "ledger": "9.1.0.0-rc.3",
    "node": "2.0.0-rc.4",
    "indexer": "4.4.0-pre-alpha.16",
    "proofServer": "9.0.0-rc.5_experimental",
    "compactCompiler": "0.33.0-rc.2",
    "compactLanguage": "0.25.0",
    "compactRuntime": "0.18.0-rc.1",
    "compactJs": "2.5.5-rc.6",
    "midnightJs": "5.0.0-beta.4",
    "walletSdk": "2.0.0-beta.2"
  },

  "selfReported": {
    "_comment": [
      "The installed compiler declares what it expects. Verified 2026-08-13 — every value",
      "agrees with the delivery note, which is the best evidence we have that this set really",
      "does move together. Re-run these four after ANY toolchain bump:",
      "  compactc --version --language-version --ledger-version --runtime-version"
    ],
    "compactc --version": "0.33.0",
    "compactc --language-version": "0.25.0",
    "compactc --ledger-version": "ledger-9.1.0.0-rc.3",
    "compactc --runtime-version": "0.18.0-rc.1"
  },

  "libraries": {
    "_comment": "Not part of the Stagenet delivery note — these are our chosen dependencies.",
    "openzeppelinCompactContracts": "0.3.0-alpha.2",
    "openzeppelinNotes": [
      "Released 2026-08-11. Modules declare `pragma language_version >= 0.23.0`, so the pinned compiler must satisfy that.",
      "Moving fast in the areas we depend on: alpha.2 removed SignerManager, dropped isInit guards in Signer, and fixed UnshieldedTreasury deposits. Re-read the multisig diff before pinning custody.",
      "archive/ShieldedToken is confirmed still archived on main as of 2026-08-13 — 'DO NOT USE IN PRODUCTION'."
    ]
  },

  "flags": ["--feature-zkir-v3"],

  "notes": {
    "zkirV3": "--feature-zkir-v3 is a COMPILER flag. Contracts built with it must run against the _experimental proof-server build — the plain build will not verify them.",
    "proofServer": "Always local, including against Stagenet.",
    "indexerApi": "GraphQL path is /api/v4/graphql on Stagenet. It is version-scoped and moves with the indexer image; a mismatch fails as an opaque 404."
  },

  "dockerTags": {
    "_comment": "Verified present on Docker Hub (midnightntwrk/*) on 2026-08-13. Live in ops/localnet/env/rc3.env.",
    "node": "2.0.0-rc.4",
    "proofServer": "9.0.0-rc.5_experimental",
    "indexerStandalone": "4.4.0-pre-alpha.16-l91r3-n2r3-bridge-and-events-epics-contract-zswap-16c656df",
    "indexerTagWarning": "There is no plain '4.4.0-pre-alpha.16' tag. The published image carries the build suffix above; shortening it gives 'manifest unknown'."
  },

  "unresolved": {
    "compactLanguageVersion": "Compiler 0.31.x maps to language 0.23.0; the 0.33.0-rc.2 release notes do not state its language version. Confirm with `compact compile --version` once the toolchain is installed.",
    "stagenetNetworkId": "The NetworkId string Stagenet expects is not documented in the delivery note. Localnet is 'undeployed'. Set MRA_NETWORK_ID until confirmed.",
    "indexerUpgrade": "indexer-standalone 4.4.0-rc.2 is published and is a clean tag, but Stagenet was tested on pre-alpha.16. Do not switch without checking node 2.0.0-rc.4 compatibility."
  }
}
