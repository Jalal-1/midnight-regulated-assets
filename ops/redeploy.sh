#!/usr/bin/env bash
# One-command redeploy. MUST survive a full localnet reset (down -v && up).
# If this ever needs a second command or manual step, that is a bug.
set -euo pipefail

echo "TODO(M0): compile contracts, deploy, write addresses where apps can read them"
echo "Budget for minutes: ~40s submit, ~70s per proved call."
exit 1
