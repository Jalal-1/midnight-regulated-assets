#!/usr/bin/env bash
# Installs the pinned Compact toolchain. The pinned set moves together —
# never upgrade one component alone. See ops/versions.lock.json.
set -euo pipefail

echo "TODO(M0): install pinned compactc + Compact stdlib from versions.lock.json"
echo "Required: --feature-zkir-v3, experimental proof server, proof server local."
exit 1
