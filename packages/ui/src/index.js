/**
 * Design system + the multi-party view.
 *
 * The multi-party view is the flagship component: "View as Issuer / Sender /
 * Receiver / Regulator / Public" — one transaction, five live queries, five
 * visibility sets. It demonstrates; it does not narrate.
 *
 * It must read live chain state per role. Any role whose view is computed
 * client-side from another role's data defeats the entire point.
 */
export {};
