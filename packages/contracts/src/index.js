/**
 * THE BLOCKS.
 *
 * Every product in apps/ is a composition of these. Nothing product-specific
 * belongs here — if it only makes sense for the deposit, it lives in
 * apps/tokenised-deposit.
 *
 *   access-control/  role gating (OZ Compact)
 *   multisig/        ECDSA 2-of-3 custody
 *   cft/             account-based confidential token
 *   note-preview/    note-based privacy (higher privacy, less mature)
 *   compliance/      transfer restrictions, allowlists, disclosure policy
 */
export {};
