/**
 * Mock core-banking ledger — the ONLY intentionally simulated component.
 *
 * Exists so mint can be 1:1 against a liability and redeem can credit an
 * account. Labelled a mock everywhere it surfaces in the UI.
 */
export {};
